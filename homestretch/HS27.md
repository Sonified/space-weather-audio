# HS27 — Background Render Ahead of Playhead

> **SHIP_IT.md:** Background render ahead of the playhead — stretch chunks before they're needed

---

## STATUS: Pipeline complete, speed refactor done, needs final study flow wiring

Background render-ahead is built and proven. The speed-everywhere refactor unblocks wavelet at any speed.

### What Exists (pipeline)
- **`stretch_test.html`** → `processAndCrossfadeGPU()`: Processes new stretch while old audio plays, then crossfades.
- **`js/audio-player.js`** → `waveletStretchAndLoad()` (line ~450): Runs GPU stretch in background and loads into wavelet AudioWorklet.
- **`js/wavelet-gpu-compute.js`** → `waveletStretchChunked()`: Auto-splits into chunks with overlap for seamless processing.

### Completed: Speed-everywhere refactor
- `baseSpeed < 1.0` gate removed — wavelet engages at any speed
- All worklets speak `speed` natively (no more stretchFactor in the protocol)

### Remaining: Study flow wiring
- Ensure render-ahead triggers during study analysis sessions when wavelet condition is active
- Background processing must start when data arrives, not wait for user to hit play
- `audio-player.js` internal stretchFactor → speed refactor (in progress)
