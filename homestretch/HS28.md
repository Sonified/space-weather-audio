# HS28 — Crossfade Between Chunks

> **SHIP_IT.md:** Crossfade between chunks for seamless playback

---

## STATUS: Pipeline complete, speed refactor done, needs final study flow wiring

Crossfade between chunks is built and proven. The speed-everywhere refactor unblocks wavelet at any speed.

### What Exists (pipeline)
- **`stretch_test.html`**: 150ms `CROSSFADE_DURATION` between chunks for seamless playback.
- **`js/wavelet-gpu-compute.js`** → `waveletStretchChunked()` (line ~2266): Built-in crossfade with overlap between chunks.
- **`js/audio-player.js`**: Crossfade integrated in main app path via `waveletStretchAndLoad()`.

### Completed: Speed-everywhere refactor
- `baseSpeed < 1.0` gate removed — wavelet engages at any speed
- All worklets speak `speed` natively (no more stretchFactor in the protocol)

### Remaining: Study flow wiring
- Crossfade logic needs to activate when study flow uses the wavelet stretch path
- Verify seamless chunk transitions work with study's data loading sequence
- `audio-player.js` internal stretchFactor → speed refactor (in progress)
