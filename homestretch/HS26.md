# HS26 — Chunked Wavelet Stretch as Data Arrives

> **SHIP_IT.md:** Process wavelet stretch in chunks as data arrives from the streaming pipeline

---

## STATUS: Pipeline complete, speed refactor done, needs final study flow wiring

The chunked wavelet stretch pipeline is built and proven. The speed-everywhere refactor is complete — all worklets, stretch_test.html, and rtpghi-wasm-loader.js now speak `speed` natively. The `baseSpeed < 1.0` gate that blocked wavelet at speeds >= 1x has been removed.

### What Exists (pipeline)
- **`js/wavelet-gpu-compute.js`** → `waveletStretchChunked()` (line ~2266): Production chunked pipeline with overlap, crossfade, progress callbacks. Auto-calculates chunk sizes for GPU memory limits.
- **`js/audio-player.js`** → `waveletStretchAndLoad()` (line ~450): Runs GPU stretch and loads into wavelet AudioWorklet. Fast path uses cached CWT coefficients (single GPU pass). Chunked fallback auto-splits long files.
- **`stretch_test.html`** (3544 lines): Complete standalone test page proving the pipeline works with 7 stretch algorithms.

### Completed: Speed-everywhere refactor
- All 5 worklet processors (`resample`, `paul`, `spectral`, `granular`, `wavelet`) refactored from `stretchFactor` → `speed`
- Worklet message protocol: `set-stretch` / `data.factor` → `set-speed` / `data.speed`
- `processorOptions: { stretchFactor }` → `processorOptions: { speed }`
- `rtpghi-wasm-loader.js`: accepts `speed`, converts to `1/speed` at the WASM binary boundary only
- `stretch_test.html`: all internal state uses `speed` — zero `stretchFactor` references remain
- **`baseSpeed < 1.0` gate removed** from `audio-player.js` — wavelet now engages at any speed (0.5x, 1.0x, 1.25x, etc.)

### Remaining: audio-player.js internal refactor + study flow wiring
- `audio-player.js` still uses `stretchFactor` internally (State, engageStretch, waveletStretchAndLoad) — needs same speed-native treatment
- `wavelet-gpu-compute.js` API still accepts `stretchFactor` — needs boundary conversion like rtpghi
- Wire `_assignedProcessing` from condition assignment through to `waveletStretchAndLoad()`
- Ensure spectrogram display updates for stretched audio
